# Copy this file to `.env` and set the real values before running Expo locally.
# Keep this file committed; never check in actual secrets.

# Required Supabase configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=public-anon-key

# Expo/eas runtime tweaks (optional until you need them)
EXPO_STAGE=development
EAS_PROJECT_ID=
AUTH_REDIRECT_URL=https://expo.dev/@your-account/employee-portal
EXPO_SCHEME=employeeportal
